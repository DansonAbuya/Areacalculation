This code calculates the area of three shapes(Rectangle, square and circle).The first code has fixed values for the shapes' properties and 
fixed outcome of the area calculations(User isn't prompted to key in properties of the shapes) while the second prompts the user to key in the shape properties.
